# demo-project

Fixture used by `npm run e2e`. It is a stand-in for "any repository", so the
toolkit can be installed, driven through a complete workflow run and observed
through the monitor without touching a real project.

The E2E script copies this directory to a temporary location before installing,
so nothing here is modified.
